# Plan Validation Report

**Spec:** 001-push-channels
**Date:** 2026-05-28
**Status:** APPROVED

---

## Step 1: Requirements Extracted from design.md

**Format validation:** All FR and AC identifiers match their required formats (`^FR#\d+$`, `^AC#\d+$`). No violations found.

### Functional Requirements (10 total)

| ID | Text (abbreviated) | Section |
|---|---|---|
| FR#1 | Server advertises push-based channel support during initialization | Functional Requirements |
| FR#2 | Server provides contextual instructions for interpreting feedback notifications, including that user-supplied text is untrusted | Functional Requirements |
| FR#3 | When user clicks "Send to Claude", server pushes notification without client polling | Functional Requirements |
| FR#4 | Screenshots saved as image files on disk; notification includes absolute file path | Functional Requirements |
| FR#5 | Screenshot storage location configurable via environment variable with sensible default | Functional Requirements |
| FR#6 | User-supplied content structurally separated from system-derived metadata in notification payload | Functional Requirements |
| FR#7 | All polling-based feedback retrieval tools removed from tool catalog | Functional Requirements |
| FR#8 | On-demand tools continue to function unchanged | Functional Requirements |
| FR#9 | Screenshot files cleaned up on server shutdown, after draining in-flight notifications | Functional Requirements |
| FR#10 | Secondary instance feedback relayed to primary instance for notification delivery | Functional Requirements |

### Acceptance Criteria (8 total)

| ID | Text (abbreviated) | Maps to |
|---|---|---|
| AC#1 | Server advertised capabilities include channel support and contextual instructions after init | FR#1, FR#2 |
| AC#2 | Push notification delivered with user-supplied text separate from system metadata, no tool call | FR#3, FR#6 |
| AC#3 | Screenshots appear as files in configured directory; notification includes absolute path | FR#4, FR#5 |
| AC#4 | Server tool catalog contains zero polling-based feedback retrieval tools | FR#7 |
| AC#5 | All on-demand tools respond correctly | FR#8 |
| AC#6 | After server shutdown, session screenshot directory is removed | FR#9 |
| AC#7 | Feedback via secondary instance results in push notification from primary | FR#10 |
| AC#8 | Server instructions explicitly identify user-supplied text as untrusted browser input | FR#2, FR#6 |

---

## Step 2: Claims Extracted from Task Files

### T01 — Add screenshots module for disk-based screenshot storage
- **depends_on:** []
- **implements:** FR#4, FR#5, AC#3
- **Verify items:** FR#4 ✓, FR#5 ✓, AC#3 ✓

### T02 — Add channel capability and pushFeedback callback
- **depends_on:** T01
- **implements:** FR#1, FR#2, FR#6, AC#1, AC#8
- **Verify items:** FR#1 ✓, FR#2 ✓, FR#6 ✓, AC#1 ✓, AC#8 ✓

### T03 — Convert WS send_to_claude handler and add widget push support
- **depends_on:** T02
- **implements:** FR#3, AC#2
- **Verify items:** FR#3 ✓, AC#2 ✓

### T04 — Delete pull-based tools and simplify session state
- **depends_on:** T03
- **implements:** FR#7, FR#8, AC#4, AC#5
- **Verify items:** FR#7 ✓, FR#8 ✓, AC#4 ✓, AC#5 ✓

### T05 — Clean up HTTP endpoints and add proxy push relay
- **depends_on:** T02, T04
- **implements:** FR#10, AC#7
- **Verify items:** FR#10 ✓, AC#7 ✓

### T06 — Add shutdown drain, startup sweep, and update documentation
- **depends_on:** T01, T02
- **implements:** FR#9, AC#6
- **Verify items:** FR#9 ✓, AC#6 ✓

### context.md Section Check

| Section | Present | Non-empty |
|---|---|---|
| Problem & Motivation | ✓ | ✓ |
| Visual Artifacts | ✓ | ✓ (explicitly "None" — valid) |
| Key Decisions | ✓ | ✓ (8 decisions listed) |
| Constraints & Anti-Patterns | ✓ | ✓ (8 constraints listed) |
| Design Doc References | ✓ | ✓ |
| Convention Examples (optional) | ✓ | ✓ |

All 5 required sections present and non-empty. ✓

---

## Step 3: Traceability Matrix

| Req ID | Covered By Task(s) | In implements field | Verify item present |
|---|---|---|---|
| FR#1 | T02 | ✓ | ✓ |
| FR#2 | T02 | ✓ | ✓ |
| FR#3 | T03 | ✓ | ✓ |
| FR#4 | T01 | ✓ | ✓ |
| FR#5 | T01 | ✓ | ✓ |
| FR#6 | T02 | ✓ | ✓ |
| FR#7 | T04 | ✓ | ✓ |
| FR#8 | T04 | ✓ | ✓ |
| FR#9 | T06 | ✓ | ✓ |
| FR#10 | T05 | ✓ | ✓ |
| AC#1 | T02 | ✓ | ✓ |
| AC#2 | T03 | ✓ | ✓ |
| AC#3 | T01 | ✓ | ✓ |
| AC#4 | T04 | ✓ | ✓ |
| AC#5 | T04 | ✓ | ✓ |
| AC#6 | T06 | ✓ | ✓ |
| AC#7 | T05 | ✓ | ✓ |
| AC#8 | T02 | ✓ | ✓ |

All 10 FRs and all 8 ACs are covered. No gaps.

---

## Step 4: Problem Analysis

### Coverage Gaps
None. Every FR and AC from the design doc appears in at least one task's `implements` field and has a corresponding Verify item.

### Contradictions
None found. All task Prompt and Focus sections align with the design doc:
- T01 correctly specifies the `saveScreenshot`/`cleanupScreenshots`/`sweepOrphanScreenshots`/`getScreenshotDir` function signatures from the Architecture section.
- T02 correctly specifies the exact `instructions` string and capability structure from the Architecture section.
- T03 correctly describes the WS handler replacement logic from the Architecture section, and includes both widget-connection changes (push_failed handler and reconnect flush) called out in the gap-check comment in design.md.
- T04 correctly lists all 5 tools to remove and all state maps to drop per the Replacement Targets table.
- T05 correctly scopes to the HTTP endpoint changes and proxy-client rewrite with `processId` validation.
- T06 correctly addresses shutdown drain, startup sweep, and documentation — the only task touching docs.

### Vague Verification Criteria
None. All Verify items are checkable against concrete artifacts:
- FR#4 and AC#3 verify against file presence on disk and returned path.
- FR#5 verifies against env var behavior.
- FR#7 and AC#4 verify against ListTools response content (named tools to check absent).
- FR#8 and AC#5 verify against on-demand tool responses.

### Dependency Graph Integrity
The dependency chain is sound:
- T01 has no dependencies (standalone I/O module).
- T02 depends on T01 (uses `saveScreenshot` inside `pushFeedback`).
- T03 depends on T02 (wires `pushFeedback` callback into WS handler).
- T04 depends on T03 (cleanup after push path is live; nothing references removed code).
- T05 depends on T02 (needs `pushFeedback` type/callback) and T04 (removes state that pull endpoints depended on).
- T06 depends on T01 and T02 (uses `cleanupScreenshots`, `sweepOrphanScreenshots`, and `pushFeedback` in-flight counter).

No circular dependencies. No task depends on a task that hasn't implemented what it needs.

### Additional Observations (non-blocking)
- T04's Focus section explicitly notes that `http-server.ts` still imports removed session-store exports and that T05 handles that cleanup. This cross-task dependency is correctly documented as a deferred cleanup rather than a gap.
- T05's `depends_on: ["T02", "T04"]` is correct — it needs the `pushFeedback` callback (T02) and the state simplification (T04) before wiring the HTTP endpoint.
- T06 does not depend on T03, T04, or T05, which is correct — it only needs the screenshot module (T01) and `pushFeedback`/in-flight tracking (T02) to be defined. The documentation updates are independent of the WS/HTTP/tool cleanup order.

---

## Step 5: Final Report

**Status: APPROVED**

- Zero coverage gaps (all 10 FRs and 8 ACs traced to implementing tasks with Verify items)
- Zero contradictions between task prompts and design doc
- context.md has all 5 required sections with non-empty content plus the optional Convention Examples section
- All FR/AC identifiers use correct format
- Dependency graph is acyclic and semantically correct
- No vague Verify criteria found
